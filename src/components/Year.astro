---
const { years } = Astro.props;
---

<div class="flex flex-wrap justify-center gap-3 py-6">
  {
    years.map((year: string) => (
      <a href={`/archive/${year}/1`} class="button">
        {year}
      </a>
    ))
  }
</div>
