---
interface Props {
  size?: "sm" | "md";
}
const { size = "sm" } = Astro.props;
const padding = size === "md" ? "px-3 py-1" : "px-2 py-0.5";
---

<span class:list={["bg-foreground/13 rounded-lg text-sm", padding]}>
  <slot />
</span>
